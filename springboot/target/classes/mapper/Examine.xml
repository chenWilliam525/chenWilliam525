<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ExamineMapper">

    <sql id="Base_Column_List">
        id, username, password, name, status
    </sql>

    <!-- 查询所有 Examine -->
    <select id="selectAll" resultType="com.example.entity.Examine">
        select
        <include refid="Base_Column_List" />
        from user
        <where>
            <if test="name != null">and name = #{name}</if>
            <if test="status != null"> and status like concat('%', #{status}, '%')</if>
        </where>
        order by id desc
    </select>

    <!-- 根据 ID 查询 -->
    <select id="selectById" resultType="com.example.entity.Examine">
        select
        <include refid="Base_Column_List" />
        from user
        where id = #{id}
    </select>

    <!-- 删除 Examine -->
    <delete id="deleteById">
        delete from user
        where id = #{id}
    </delete>

    <!-- 新增 Examine -->
    <insert id="insert" parameterType="com.example.entity.Examine" useGeneratedKeys="true">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="username != null">username,</if>
            <if test="password != null">password,</if>
            <if test="name != null">name,</if>
            <if test="status != null">status,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="username != null">#{username},</if>
            <if test="password != null">#{password},</if>
            <if test="name != null">#{name},</if>
            <if test="status != null">#{status},</if>
        </trim>
    </insert>

    <!-- 更新 Examine -->
    <update id="updateById" parameterType="com.example.entity.Examine">
        update user
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>
